import { BusStop } from "../database/db-models/BusStop";

export class CityGraph{
    public stops: BusStop[] = []

    public addBusStop(busStopName: string, lon: number, lat: number){
    }
    public getBusStop(busStopName: string){
        
    }
}